.venv/
__pycache__/
*.pyc
.ruff_cache/
.pytest_cache/
.smith/
knowledge/cache/*
!knowledge/cache/.gitignore
knowledge/MANIFEST.json
knowledge/DRIFT.md

# transient subagent prompts written by 'smith delegate'
assignments/
backups/
state/session.json
state/session/

# JUnit XML reports generated by 'pytest --junitxml=...' (see ci.yml and
# justfile). One per matrix cell in CI, uploaded as an artifact there rather
# than committed.
test-results*.xml

